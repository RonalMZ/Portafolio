# OpenAI API key for the AI chatbot in the contact section.
# The site works without it (a smart local fallback is used),
# but with a key the chatbot uses GPT-4o-mini to answer.
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=
